import styled from '@emotion/styled';

const StyledAudioWrapper = styled.div`
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 15px;
`;

const StyledAudio = styled.audio`
    width: 100%;
`;

export { StyledAudioWrapper, StyledAudio };
